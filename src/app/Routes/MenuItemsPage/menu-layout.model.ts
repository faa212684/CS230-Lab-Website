export class MenuLayoutModel {
    img: string;
    header: string;
    button: string;

    constructor(img: string, header: string, button: string) {
        this.img = img;
        this.header = header;
        this.button = button;
    }
}