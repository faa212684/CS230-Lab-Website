export class MenuItemModel {
    img: string;
    header: string;
    statement: string;

    constructor(img: string, header: string, statement: string){
        this.img = img;
        this.header = header;
        this.statement = statement;
    }
}